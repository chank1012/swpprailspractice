<h1>Listing articles</h1>

<table>
  <tr>
    <th>#</th>
    <th>Title</th>
    <th>Text</th>
  </tr>

  <% @articles.each do |a| %>
  <tr>
    <td><%= a.id %></td>
    <td><%= a.title %></td>
    <td><%= a.text %></td>
  </tr>
  <% end %>
</table>

<%= link_to 'New article', new_article_path %>
